using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class SaveManager : MonoBehaviour
{
    /*
    //Also save when player get's GameOver()
    void Start()        //Instead save after changing SaveData.current.LvlManager.whatever, --and-- when I load x = SaveData.current.LvlManager.whatever
    {
        SerializationManager.Save("Save", SaveData.current);
        Debug.Log("Save");
    }*/
}
